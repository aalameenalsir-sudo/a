const locale=document.documentElement.lang==='ar'?'ar':'en',form=document.querySelector('#track-form'),box=document.querySelector('#track-result');form?.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form));box.textContent=locale==='ar'?'جاري البحث...':'Checking...';try{const o=await ASolutionRemote.trackOrder(d.order_number,d.email);box.innerHTML=`<div class="success"><b>${o.order_number}</b><br>${locale==='ar'?'الدفع':'Payment'}: ${o.payment_status}<br>${locale==='ar'?'التنفيذ':'Fulfillment'}: ${o.fulfillment_status}<br>${locale==='ar'?'الإجمالي':'Total'}: ${ASolutionStore.formatMoney(o.total,locale)}</div>`}catch(x){box.innerHTML=`<p class="error">${x.message}</p>`}});