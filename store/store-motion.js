(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const reveals=[...document.querySelectorAll('[data-reveal],.mask-reveal')];reveals.forEach((el,i)=>el.style.setProperty('--reveal-delay',`${Math.min(i%4,3)*55}ms`));if(reduced){reveals.forEach(x=>x.classList.add('is-visible'));return}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -5%'});reveals.forEach(x=>io.observe(x));const media=[...document.querySelectorAll('.parallax-media')];let ticking=false;function move(){const h=innerHeight;media.forEach(el=>{const r=el.parentElement.getBoundingClientRect(),p=(r.top+h)/(h+r.height);el.style.transform=`translate3d(0,${(p-.5)*28}px,0) scale(1.025)`});ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(move);ticking=true}},{passive:true});move();document.querySelectorAll('.magnetic-cta').forEach(btn=>{btn.addEventListener('pointermove',e=>{const r=btn.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.08,y=(e.clientY-r.top-r.height/2)*.12;btn.style.setProperty('--mx',`${x}px`);btn.style.setProperty('--my',`${y}px`)});btn.addEventListener('pointerleave',()=>{btn.style.setProperty('--mx','0px');btn.style.setProperty('--my','0px')})});const bar=document.querySelector('.scroll-progress');function progress(){if(!bar)return;const d=document.documentElement,m=d.scrollHeight-d.clientHeight,p=m?d.scrollTop/m:0;bar.style.background=`linear-gradient(90deg,var(--ps-accent) ${p*100}%,transparent ${p*100}%)`}addEventListener('scroll',progress,{passive:true});progress()})();
