const locale=document.documentElement.lang==='ar'?'ar':'en',form=document.querySelector('#review-form'),status=document.querySelector('#review-status');form?.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form));try{const products=await ASolutionRemote.listProducts();const p=products.find(x=>x.slug===d.product);if(!p)throw new Error('Product not found');await ASolutionRemote.submitReview(p.id,d.rating,d.title,d.body);status.textContent=locale==='ar'?'تم إرسال التقييم للمراجعة.':'Review submitted for moderation.';form.reset()}catch(x){status.textContent=x.message==='LOGIN_REQUIRED'?(locale==='ar'?'سجل الدخول أولاً.':'Please sign in first.'):x.message}});