const status=document.querySelector('#auth-status');document.querySelector('#login-form')?.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));try{await ASolutionRemote.login(d.email,d.password);location.href='account.html'}catch(x){status.textContent=x.message}});document.querySelector('#signup-form')?.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));try{const r=await ASolutionRemote.signup(d.email,d.password,{name:d.name});status.textContent=r?.access_token?'Account created.':'Check your email to confirm your account.'}catch(x){status.textContent=x.message}});