(async()=>{const A=ASolutionAdmin,esc=A.escapeHTML,id=new URLSearchParams(location.search).get('id'),status=document.querySelector('#detail-status');let role=null,order=null;try{role=await A.guard()}catch(e){A.showError(status,e);return}if(!id){A.showError(status,'Missing order ID');return}async function load(){const [orders,items,payments,shipments]=await Promise.all([A.req('commerce_orders',`?id=eq.${encodeURIComponent(id)}&select=*`),A.req('commerce_order_items',`?order_id=eq.${encodeURIComponent(id)}&select=*`),A.req('commerce_payments',`?order_id=eq.${encodeURIComponent(id)}&select=*&order=created_at.desc`),A.req('commerce_shipments',`?order_id=eq.${encodeURIComponent(id)}&select=*&order=shipped_at.desc.nullslast`)]);order=orders?.[0];if(!order)throw new Error('Order not found');document.querySelector('#order-title').textContent=order.order_number;document.querySelector('#order-items').innerHTML=items.map(x=>`<div class="line"><span>${esc(x.name)} × ${x.quantity}<br><small>${esc(x.product_type||'')}</small></span><b>${order.currency} ${Number(x.line_total).toLocaleString()}</b></div>`).join('');document.querySelector('#customer').innerHTML=`<p><b>${esc(order.name)}</b><br>${esc(order.email)}<br>${esc(order.phone||'')}</p><p>${esc(order.address_line||'')}<br>${esc(order.city||'')}</p><p>${esc(order.notes||'')}</p>`;document.querySelector('#totals').innerHTML=[['Subtotal',order.subtotal],['Discount',-Number(order.discount||0)],['Shipping',order.shipping],['VAT',order.vat],['Total',order.total]].map(([n,v])=>`<div class="line"><span>${n}</span><b>${order.currency} ${Number(v||0).toLocaleString()}</b></div>`).join('');document.querySelector('#status-form').order_status.value=order.order_status||'open';document.querySelector('#status-form').fulfillment_status.value=order.fulfillment_status;document.querySelector('#payment-info').innerHTML=`<p>Method: <b>${esc(order.payment_method)}</b><br>Status: <b>${esc(order.payment_status)}</b></p>`+payments.map(p=>`<small>${esc(p.provider)} · ${esc(p.status)} · ${order.currency} ${Number(p.amount).toLocaleString()}</small><br>`).join('');const s=shipments?.[0];if(s){const f=document.querySelector('#shipment-form');f.provider.value=s.provider||'';f.tracking_number.value=s.tracking_number||'';f.tracking_url.value=s.tracking_url||'';f.status.value=s.status||'pending'}document.querySelector('#finance-card').hidden=!(A.can('finance.write',role)||['owner','admin'].includes(role));}
document.querySelector('#status-form').addEventListener('submit',async e=>{e.preventDefault();if(!(A.can('orders.write',role)||['owner','admin'].includes(role)))return alert('No permission');const d=Object.fromEntries(new FormData(e.currentTarget));await A.req('commerce_orders',`?id=eq.${id}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({...d,updated_at:new Date().toISOString()})});status.textContent='Status saved.';load()});document.querySelector('#shipment-form').addEventListener('submit',async e=>{e.preventDefault();if(!(A.can('orders.write',role)||['owner','admin'].includes(role)))return alert('No permission');const d=Object.fromEntries(new FormData(e.currentTarget)),existing=(await A.req('commerce_shipments',`?order_id=eq.${id}&select=id&limit=1`))?.[0],payload={order_id:id,provider:d.provider||null,tracking_number:d.tracking_number||null,tracking_url:d.tracking_url||null,status:d.status,shipped_at:d.status==='shipped'?new Date().toISOString():null,delivered_at:d.status==='delivered'?new Date().toISOString():null};if(existing)await A.req('commerce_shipments',`?id=eq.${existing.id}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(payload)});else await A.req('commerce_shipments','',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(payload)});status.textContent='Shipment saved.';load()});document.querySelector('#refund-form').addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget)),amount=Number(d.amount);if(!amount||amount<=0)return alert('Enter a refund amount');if(!confirm(`Refund ${order.currency} ${amount}?`))return;const r=await A.request('/functions/v1/commerce-refund',{method:'POST',body:JSON.stringify({order_id:id,amount,reason:d.reason||''})});status.textContent=`Refund created: ${r.refund?.status||'ok'}`;e.currentTarget.reset();load()});load().catch(e=>A.showError(status,e));})();
