(async()=>{try{await ASolutionAdmin.guard();const [orders,products,customers]=await Promise.all([ASolutionAdmin.req('commerce_orders','?select=*&order=created_at.desc&limit=20'),ASolutionAdmin.req('commerce_products','?select=id,name_en,inventory_qty,track_inventory,active'),ASolutionAdmin.req('commerce_customers','?select=id,lifetime_value')]);const revenue=orders.filter(o=>o.payment_status==='paid').reduce((s,o)=>s+Number(o.total),0),open=orders.filter(o=>!['fulfilled','cancelled','returned'].includes(o.fulfillment_status)).length,ltv=customers.reduce((s,c)=>s+Number(c.lifetime_value||0),0);document.querySelector('#admin-kpis').innerHTML=`<div class="kpi"><small>REVENUE</small><b>SAR ${revenue.toLocaleString()}</b></div><div class="kpi"><small>ORDERS</small><b>${orders.length}</b></div><div class="kpi"><small>OPEN</small><b>${open}</b></div><div class="kpi"><small>CUSTOMER LTV</small><b>SAR ${ltv.toLocaleString()}</b></div>`;document.querySelector('#recent-orders').innerHTML=orders.slice(0,8).map(o=>`<div class="list-row"><b>${o.order_number}</b><span>${o.name}</span><span>SAR ${Number(o.total).toLocaleString()}</span><span>${o.fulfillment_status}</span></div>`).join('')||'<p>No orders yet.</p>';const low=products.filter(p=>p.track_inventory&&Number(p.inventory_qty)<=5);document.querySelector('#attention').innerHTML=low.length?low.map(p=>`<div class="list-row"><b>${p.name_en}</b><span>Low stock: ${p.inventory_qty}</span></div>`).join(''):'<p>No inventory alerts.</p>'}catch(e){console.error(e)}})();