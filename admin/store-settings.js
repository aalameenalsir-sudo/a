const form=document.querySelector('#settings-form'),status=document.querySelector('#settings-status');let id=null;(async()=>{try{await ASolutionAdmin.guard();const rows=await ASolutionAdmin.req('commerce_settings','?select=*&limit=1');const s=rows[0];if(s){id=s.id;for(const [k,v] of Object.entries(s)){if(form.elements[k])form.elements[k].value=v??''}}}catch(e){status.textContent=e.message}})();form.onsubmit=async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(form));d.vat_rate=Number(d.vat_rate||.15);if(id)await ASolutionAdmin.req('commerce_settings',`?id=eq.${id}`,{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify(d)});else await ASolutionAdmin.req('commerce_settings','',{method:'POST',body:JSON.stringify({...d,singleton:true})});status.textContent='Saved.'}catch(x){status.textContent=x.message}};