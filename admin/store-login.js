(function(){'use strict';const cfg={url:'https://bgxtcpcbkjftkgswpizr.supabase.co',key:'sb_publishable_t9MKvlsLheyCj-o-jIQ04A_9sGI_j_r'},client=window.supabase.createClient(cfg.url,cfg.key),form=document.querySelector('#store-login-form'),status=document.querySelector('#store-login-status');function safeNext(){const raw=new URLSearchParams(location.search).get('next')||'store-dashboard.html';return /^store-[a-z0-9-]+\.html(?:[?#].*)?$/i.test(raw)?raw:'store-dashboard.html'}async function allowed(userId){const {data:staff}=await client.from('commerce_staff_roles').select('role,active').eq('user_id',userId).maybeSingle();if(staff?.active&&staff.role)return true;const {data:profile}=await client.from('profiles').select('role').eq('id',userId).maybeSingle();return profile?.role==='admin'}(async()=>{const {data}=await client.auth.getSession();if(data?.session&&await allowed(data.session.user.id))location.replace(safeNext())})();form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Signing in…';const d=Object.fromEntries(new FormData(form)),{data,error}=await client.auth.signInWithPassword({email:String(d.email||'').trim(),password:String(d.password||'')});if(error||!data?.user){status.textContent=error?.message||'Unable to sign in.';return}if(!await allowed(data.user.id)){await client.auth.signOut();status.textContent='This account does not have store staff access.';return}location.replace(safeNext())});})();
